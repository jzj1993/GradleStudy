apply plugin: 'java'

dependencies {
//    compile 'org.codehaus.groovy:groovy-all:2.3.11'
    compile localGroovy() // Groovy支持(本地)
    compile gradleApi() // GradleAPI支持
    compile 'com.android.tools.build:gradle:3.0.1' // Android插件
}
